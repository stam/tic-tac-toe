import styled from 'styled-components';

const Button = styled.button`
  cursor: pointer;
  padding: 0.25rem 0;
  border-radius: 4px;
  font-size: 2rem;
  background: blue;
  color: white;
  border: none;
  outline: none;
`;

export default Button;
